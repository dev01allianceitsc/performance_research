/**
 * Backend URL configuration.
 *
 * Local dev:  Leave empty — frontend auto-connects to localhost:3001.
 * Production: Set window.__BACKEND_URL__ to your Railway deployment URL.
 *
 * Railway deployment URL format: https://your-app.up.railway.app
 *
 * On Vercel, create a new env.js via "Deployment Configuration" or
 * inject it at build time using Vercel environment variables.
 */
window.__BACKEND_URL__ = '';  // e.g. 'https://server-bench.up.railway.app'
