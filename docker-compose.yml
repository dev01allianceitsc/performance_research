version: '3.9'

services:
  server-bench:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
      - NODE_ENV=production
    restart: unless-stopped
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
